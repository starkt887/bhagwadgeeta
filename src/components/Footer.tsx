import { IonFooter, IonIcon, IonLabel, IonTabBar, IonTabButton, IonTabs, IonTitle, IonToolbar } from '@ionic/react'
import React from 'react'

import { home as HomeIcon, settings as SettingsIcon, statsChart as ProgressIcon, saveSharp as saveIcon } from 'ionicons/icons';


const Footer = () => {
    return (
        <IonFooter>
            {/* <IonToolbar>
                <IonTitle>Footer Toolbar</IonTitle>
            </IonToolbar> */}
           
        </IonFooter>
    )
}

export default Footer